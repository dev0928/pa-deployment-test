name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        
permissions:
  contents: write
jobs:
  Release-solution-to-production:
    uses: ./.github/workflows/release-solution-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: PowerAppsDeployment
      #Update your values here
      BUILD_ENVIRONMENT_URL: "https://org74c7a618.crm.dynamics.com"
      PRODUCTION_ENVIRONMENT_URL: "https://org8dbfc283.crm.dynamics.com"
      USERNAME: "SakuRamamoorthy@cu0320.onmicrosoft.com"
    secrets:
      envSecret: ${{secrets.PASSWORD}}
